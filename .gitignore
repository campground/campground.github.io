_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
.bundle/
vendor/
.DS_Store
node_modules/
assets/css/site.css

# Impeccable platform binary: launcher downloads it on demand
.claude/skills/impeccable/scripts/bin/

# Impeccable live-mode session state (config.json is kept)
.impeccable/live/*
!.impeccable/live/config.json

# Impeccable build-phase state and review captures (session artifacts)
.impeccable/build/
.impeccable/review/

# Impeccable design-hook session cache
.impeccable/hook.cache.json

# Lighthouse CI local runs
.lighthouseci/
